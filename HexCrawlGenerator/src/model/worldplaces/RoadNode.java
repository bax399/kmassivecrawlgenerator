package model.worldplaces;
import model.Point;
import model.RoadNetwork;

public class RoadNode extends NetworkNode<RoadNetwork>
{
	public RoadNode(RoadNetwork rn, Point p)
	{
		super(rn,p);
	}
}
