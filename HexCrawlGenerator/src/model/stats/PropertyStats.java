package model.stats;
import java.util.*;

/**
 * @author Keeley
 * Abstract class for the _Stats objects to inherit from. The PropertiesFactory reads in Properties, and processes them into _Stats objects.
 * WorldElements HAVE a _Stats objects that contains it generation and base attributes. It then has information based on position, randomized-substats.
 * Stats are used by the Generator object to create the corresponding WorldElement object.
 */
public abstract class PropertyStats 
{

}
